namespace TournamentManager.Models
{
    public enum TournamentStatus
    {
        Open = 0,
        Closed = 1,
        PendingBracket = 2,
        InProgress = 3,
        Completed = 4,
        Deleted = 5
    }
}
